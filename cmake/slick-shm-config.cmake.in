@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/slick-shm-targets.cmake")

if(NOT TARGET slick::slick_shm AND TARGET slick::shm)
    add_library(slick::slick_shm ALIAS slick::shm)
endif()

check_required_components(slick-shm)
